#include <RcppArmadillo.h>
// #include <SLOPE.h>
using namespace Rcpp;

// // [[Rcpp::export]]
// arma::mat prox_sorted_L1(arma::mat& m, const arma::vec& lambda) {
//   return(SLOPE::prox(m, lambda));
// }
